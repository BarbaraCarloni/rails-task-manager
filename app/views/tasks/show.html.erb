<h1><%= @task.title %></h1>
<h3><%= @task.details %></h3>

<h3><%= "This task is not complet yet "%></h3>

<%= link_to "Back", tasks_path %> |
<%= link_to "Edit ", edit_path(@task) %> |
<%= link_to "Delete", task_path(@task),
 data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
